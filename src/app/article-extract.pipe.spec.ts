import { ArticleExtractPipe } from './article-extract.pipe';

describe('ArticleExtractPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleExtractPipe();
    expect(pipe).toBeTruthy();
  });
});
